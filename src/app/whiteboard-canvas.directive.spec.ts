import { WhiteboardCanvasDirective } from './whiteboard-canvas.directive';

describe('WhiteboardCanvasDirective', () => {
  it('should create an instance', () => {
    const directive = new WhiteboardCanvasDirective();
    expect(directive).toBeTruthy();
  });
});
